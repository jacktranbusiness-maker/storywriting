# Push this folder to GitHub

Cloud agent could not create `jacktranbusiness-maker/omni-flash-story-v1` (token scope). Run locally:

```bash
cd omni-flash-story-v1
gh repo create jacktranbusiness-maker/omni-flash-story-v1 --public --source=. --remote=origin --push
```

Or on GitHub: **New repository** → name `omni-flash-story-v1` → then:

```bash
git remote add origin https://github.com/jacktranbusiness-maker/omni-flash-story-v1.git
git push -u origin main
```
